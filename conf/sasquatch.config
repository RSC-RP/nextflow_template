// Settings to run the workflow on Sasquatch
params {
  assoc = null
}

//working directory for temporary/intermediate files produced in the workflow processes
workDir = "/data/hps/assoc/private/${params.assoc}/user/${System.getenv("USER")}/temp"

profiles{
    sasquatch {
        process.executor = 'slurm'
        process.queue = 'cpu-core'
        process.memory = 7500.MB
        process.time = '72h'
        process.clusterOptions = "--account ${params.assoc}"
        docker.enabled = false
        params.enable_conda = false
    }
}

singularity {
    enabled = true
    autoMounts = true
    cacheDir = "/data/hps/assoc/private/${params.assoc}/container"
    runOptions = '--containall'
}


executor {
    queueSize = 2000
}
